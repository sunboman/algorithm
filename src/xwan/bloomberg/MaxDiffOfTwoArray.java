package xwan.bloomberg;

/**
 * Created by xwan on 1/3/17.
 */

/**
 * give two array A[], B[], get a, b from each array, calculate the max a - b (index is different)
 */
public class MaxDiffOfTwoArray {
    public static int maxDiff(int[] a, int[] b) {
        if (a == null || b == null || a.length == 0 || b.length == 0) {
            return -1;
        }
        int maxDiff = Integer.MIN_VALUE;


        return maxDiff;
    }

    public static void main(String[] args) {
        StringBuilder sb = new StringBuilder();
        sb.append("1").append("2");
        System.out.println(sb.indexOf("9"));
    }
}
